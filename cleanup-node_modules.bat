@echo off
cd /d "C:\Users\byeor\Claude\Projects\이야기 도우미"
echo === removing node_modules from git tracking (local files are kept) ===
git rm -r --cached node_modules
echo rm exit code: %errorlevel%
echo === git add ===
git add .
echo add exit code: %errorlevel%
echo === git commit ===
git commit -m "chore: stop tracking node_modules (gitignore)"
echo commit exit code: %errorlevel%
echo === git push ===
git push
echo push exit code: %errorlevel%
pause
